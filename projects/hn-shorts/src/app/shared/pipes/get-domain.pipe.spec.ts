import { GetDomainPipe } from './get-domain.pipe';

describe('GetDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
